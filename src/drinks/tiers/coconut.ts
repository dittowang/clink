import type { DrinkVisual } from '../types'
import { placeholderDrink } from '../placeholder'

/** Tier 7 — coconut. STUB: replaced by the real procedural build in the drinks milestone. */
export function buildCoconut(): DrinkVisual {
  return placeholderDrink(7)
}
