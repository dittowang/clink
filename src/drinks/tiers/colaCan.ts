import type { DrinkVisual } from '../types'
import { placeholderDrink } from '../placeholder'

/** Tier 3 — colaCan. STUB: replaced by the real procedural build in the drinks milestone. */
export function buildColaCan(): DrinkVisual {
  return placeholderDrink(3)
}
