import type { DrinkVisual } from '../types'
import { placeholderDrink } from '../placeholder'

/** Tier 12 — dispenser. STUB: replaced by the real procedural build in the drinks milestone. */
export function buildDispenser(): DrinkVisual {
  return placeholderDrink(12)
}
