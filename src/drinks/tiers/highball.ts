import type { DrinkVisual } from '../types'
import { placeholderDrink } from '../placeholder'

/** Tier 5 — highball. STUB: replaced by the real procedural build in the drinks milestone. */
export function buildHighball(): DrinkVisual {
  return placeholderDrink(5)
}
