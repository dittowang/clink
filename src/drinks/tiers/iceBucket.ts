import type { DrinkVisual } from '../types'
import { placeholderDrink } from '../placeholder'

/** Tier 11 — iceBucket. STUB: replaced by the real procedural build in the drinks milestone. */
export function buildIceBucket(): DrinkVisual {
  return placeholderDrink(11)
}
