import type { DrinkVisual } from '../types'
import { placeholderDrink } from '../placeholder'

/** Tier 1 — juiceBox. STUB: replaced by the real procedural build in the drinks milestone. */
export function buildJuiceBox(): DrinkVisual {
  return placeholderDrink(1)
}
