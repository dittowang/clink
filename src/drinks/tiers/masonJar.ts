import type { DrinkVisual } from '../types'
import { placeholderDrink } from '../placeholder'

/** Tier 6 — masonJar. STUB: replaced by the real procedural build in the drinks milestone. */
export function buildMasonJar(): DrinkVisual {
  return placeholderDrink(6)
}
