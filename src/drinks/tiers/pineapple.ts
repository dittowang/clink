import type { DrinkVisual } from '../types'
import { placeholderDrink } from '../placeholder'

/** Tier 8 — pineapple. STUB: replaced by the real procedural build in the drinks milestone. */
export function buildPineapple(): DrinkVisual {
  return placeholderDrink(8)
}
