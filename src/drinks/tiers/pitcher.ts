import type { DrinkVisual } from '../types'
import { placeholderDrink } from '../placeholder'

/** Tier 9 — pitcher. STUB: replaced by the real procedural build in the drinks milestone. */
export function buildPitcher(): DrinkVisual {
  return placeholderDrink(9)
}
