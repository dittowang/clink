import type { DrinkVisual } from '../types'
import { placeholderDrink } from '../placeholder'

/** Tier 2 — slimCan. STUB: replaced by the real procedural build in the drinks milestone. */
export function buildSlimCan(): DrinkVisual {
  return placeholderDrink(2)
}
