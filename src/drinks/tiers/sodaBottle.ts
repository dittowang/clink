import type { DrinkVisual } from '../types'
import { placeholderDrink } from '../placeholder'

/** Tier 4 — sodaBottle. STUB: replaced by the real procedural build in the drinks milestone. */
export function buildSodaBottle(): DrinkVisual {
  return placeholderDrink(4)
}
