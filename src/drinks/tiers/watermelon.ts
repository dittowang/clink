import type { DrinkVisual } from '../types'
import { placeholderDrink } from '../placeholder'

/** Tier 10 — watermelon. STUB: replaced by the real procedural build in the drinks milestone. */
export function buildWatermelon(): DrinkVisual {
  return placeholderDrink(10)
}
